NodeOUs:
  Enable: true
  ClientOUIdentifier:
    Certificate: intermediatecerts/ca.${HOSTNAME}.${DOMAIN}-cert.pem
    OrganizationalUnitIdentifier: client
  PeerOUIdentifier:
    Certificate: intermediatecerts/ca.${HOSTNAME}.${DOMAIN}-cert.pem
    OrganizationalUnitIdentifier: peer
  AdminOUIdentifier:
    Certificate: intermediatecerts/ca.${HOSTNAME}.${DOMAIN}-cert.pem
    OrganizationalUnitIdentifier: admin
  OrdererOUIdentifier:
    Certificate: intermediatecerts/ca.${HOSTNAME}.${DOMAIN}-cert.pem
    OrganizationalUnitIdentifier: orderer
